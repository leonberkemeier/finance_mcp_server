# Example .env for local development. Copy to `.env` and edit as needed.
# Use four slashes for an absolute sqlite path: sqlite:////absolute/path/to/financial_data.db
DATABASE_URL=sqlite:////path/to/your/data.db
